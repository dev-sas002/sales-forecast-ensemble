# Test and dev dependencies.
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# fastapi.testclient is a re-export of starlette's, which needs httpx. It was
# missing, so tests/test_serving.py failed at *collection* — taking the whole
# suite down with it, not just its own tests.
httpx>=0.27.0

ruff>=0.5.0
